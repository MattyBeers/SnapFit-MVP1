# server/.env.example - placeholders only. DO NOT COMMIT REAL SECRETS
# Copy this to `server/.env` locally and fill in real values (never commit).

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<DB_USER>:<URL_ENCODED_PASSWORD>@<CLUSTER_HOST>/<DB_NAME>?retryWrites=true&w=majority
MONGODB_DB_NAME=snapfit

# Supabase (public anon key safe for client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=public-anon-key

# Third-party APIs (server-side secrets)
OPENAI_API_KEY=sk-...
REMOVEBG_API_KEY=your-removebg-key
HUGGINGFACE_API_KEY=hf-...

# Scraper (server-only)
SCRAPING_PROVIDER=scrapingbee
SCRAPING_API_KEY=your-scraping-api-key

# Server settings
CORS_ORIGIN=https://your-frontend.onrender.com
NODE_ENV=production
PORT=3001
# Server Environment Variables
# Copy this to .env and fill in your values

# MongoDB
MONGODB_URI=your_mongodb_connection_string_here
MONGODB_DB_NAME=snapfit

# Supabase (for JWT verification)
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
